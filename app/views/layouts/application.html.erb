<!DOCTYPE html>
<html>
  <head>
    <title>Places Final</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="/stylesheets/application.css">
  </head>

  <body>

    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Travel Log</a>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">

            <a class="nav-link" aria-current="page" href="/">Home</a>

            <!-- find user -->
            <% @user = User.find_by({ "id" => session["user_id"] }) %>
            
            <% if @user == nil %>
              <!-- if no user logged-in, display signup and login links -->
              <a class="nav-link" aria-current="page" href="/users/new">Sign Up</a>

              <a class="nav-link" aria-current="page" href="/login">Login</a>
            <% else %>
              <!-- else, if user logged-in, display username and logout link -->
              <a class="nav-link" aria-current="page"> Welcome, <%= @user["username"] %></a>

              <a class="nav-link" aria-current="page" href="/logout">Logout</a>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div>
      <%= flash["notice"] %>
    </div>

    <div class = "container-fluid">
      <%= yield %>  
    </div>

    
  </body>
</html>
